import { InputPhoneDirective } from './input-phone.directive';

describe('InputPhoneDirective', () => {
  it('should create an instance', () => {
    const directive = new InputPhoneDirective();
    expect(directive).toBeTruthy();
  });
});
